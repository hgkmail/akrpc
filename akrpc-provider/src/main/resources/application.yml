spring:
  application:
    name: akrpc-provider

server:
  port: 8081

akrpc:
  discovery:
    type: consul
    address: localhost:8500

management:
  endpoints:
    web:
      exposure:
        include: health, info, beans